{
  "name": "graphql-project",
  "version": "1.0.0",
  "description": "Project",
  "main": "index.js",
  "scripts": {
    "start:dev": "concurrently \"cd client && npm start\" \"cd server && npm run dev\"",
    "build": "cd client && npm run build",
    "install": "cd client && npm install && cd server && npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git@me.github.com:farzanapomy/GraphQl-project.git"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^7.0.0"
  }
}
